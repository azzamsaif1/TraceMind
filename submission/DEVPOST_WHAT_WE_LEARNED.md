# What we learned

Building trust is a product feature. The difference between a demo and a product is whether
every visible number traces to a real operation and whether the system fails honestly. We
also learned that precision in change-impact comes from *semantics* — modelling what a change
means and where it can travel — not from bigger models.
